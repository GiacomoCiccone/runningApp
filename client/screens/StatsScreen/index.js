import * as React from 'react';
import * as RN from 'react-native';

const StatsScreen = () => {

    return (
        <RN.View style={{backgroundColor: 'blue', flex: 1}}>
            
        </RN.View>
    );
}

const styles = RN.StyleSheet.create({})

export default StatsScreen;